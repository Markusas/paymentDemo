server:
  port: 8081

spring:
  h2:
    console:
      enabled: true
      path: /console
  datasource:
    url: jdbc:h2:mem:payment
    username: sa
    password:
    driverClassName: org.h2.Driver
    platform: h2
